#include<stdio.h>
int main()
{
   int n1=1,n2=1,n3,i,num;
   printf("Enter the number:");
   scanf("%d",&num);
   printf("%d\t%d\t",n1,n2);
   for(i=2;i<num;++i)
   {
      n3=n1+n2;
      printf("%d\t",n3);
      n1=n2;
      n2=n3;
   }
   return 0;
}
